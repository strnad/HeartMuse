# HeartMuse - Default Configuration
# Copy this file to .env and adjust values as needed.
# All variables are optional - defaults are used when not set.

# --- LLM Backend ---
# LLM_BACKEND=Ollama          # Ollama or OpenAI

# --- Ollama ---
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=glm-4.7-flash

# --- OpenAI ---
# OPENAI_API_KEY=sk-...
# OPENAI_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4.1-mini
# OPENAI_MODELS=gpt-4o-mini,gpt-4o,gpt-4.1-mini,gpt-4.1,o4-mini,gpt-5-mini,gpt-5,gpt-5.2

# --- LLM Generation ---
# LLM_TEMPERATURE=0.7
# LLM_TIMEOUT=120           # Timeout in seconds for LLM API calls

# --- Music Generation ---
# MUSIC_TEMPERATURE=1.0
# MUSIC_CFG_SCALE=1.5
# MUSIC_TOPK=50
# MUSIC_MAX_LENGTH_SEC=240

# --- Model Selection ---
# MODEL_VARIANT=rl             # rl or base (RL recommended for better style/tag control)

# --- Lazy Loading ---
# Load models on demand and free VRAM between generation stages.
# LAZY_LOAD=true

# --- AudioSR Upscaling ---
# Upscale generated audio to 48kHz using AudioSR super-resolution model
# AUDIOSR_ENABLED=false        # Auto-upscale after music generation
# AUDIOSR_DDIM_STEPS=50        # Diffusion steps (10-500, higher=better quality but slower)
# AUDIOSR_GUIDANCE_SCALE=3.5   # Guidance scale (1.0-20.0)
# AUDIOSR_SEED=42              # Random seed for reproducibility
# AUDIOSR_FORMAT=mp3            # Output format: mp3, flac, wav

# --- Server ---
# SERVER_HOST=127.0.0.1     # Use 0.0.0.0 to listen on all interfaces
# SERVER_PORT=7860

# --- Paths ---
# CKPT_DIR=./ckpt
# OUTPUT_DIR=./output
